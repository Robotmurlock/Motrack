"""Hydra CLI entry points for Motrack tools.

After ``pip install -e .`` (or ``uv sync``) the following console scripts
are available from any directory:

- ``motrack-inference``
- ``motrack-eval``
- ``motrack-optimize``

You can also invoke them as modules: ``python -m motrack.cli.inference``.
"""
