name: sort
params:
  filter_name: bot-sort
  filter_params:
    override_std_weight_position: 0.05
  initialization_threshold: 3
  matcher_algorithm: move
  matcher_params:
    distance_name: l1
    match_threshold: 0.2
    motion_lambda: 5
    only_matched: false
  remember_threshold: 30